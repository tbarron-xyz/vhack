name: Given system prompts file, write vulnerability testing strategies

on:
  workflow_dispatch:

jobs:
  analyze:
    uses: ./.github/workflows/analyze-tools-from-repo.yml
    with:
      scanner_model: openai/gpt-5-nano
    secrets:
      OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}