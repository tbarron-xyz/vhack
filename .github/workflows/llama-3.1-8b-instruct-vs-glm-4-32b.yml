name: "llama-3.1-8b-instruct -vs-> glm-4-32b"

on:
  workflow_run:
    workflows: ["Generate Prompts for Llama-3.1-8B-Instruct"]
    types: [completed]

jobs:
  test:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    uses: ./.github/workflows/test-prompts.yml
    with:
      scanner_model: meta-llama/llama-3.1-8b-instruct
      vhack_model: z-ai/glm-4-32b
      generate_run_id: ${{ github.event.workflow_run.id }}
    secrets:
      OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}