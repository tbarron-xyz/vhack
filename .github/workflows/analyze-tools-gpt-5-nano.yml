name: Analyze tool vulnerabilities with GPT-5-Nano

on:
  # push:
  #   branches: [main, build-action]
  workflow_dispatch:

jobs:
  analyze:
    uses: ./.github/workflows/analyze-tools-only.yml
    with:
      scanner_model: openai/gpt-5-nano
    secrets:
      OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}